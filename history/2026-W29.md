# 2026-W29  (2026-07-13 – 2026-07-19)

## 2026-07-15
### [code] lib/src/organisms/ui_adaptive_nav_shell.dart · test/ · docs/ · .claude/skills/flutter-adaptive-navigation/ — adaptive navigation shell (v0.5.0)
- **What:** Harvested an external "Flutter Complete Adaptive Layout Guide" into kit-conformant form
  (docs + component + skill), reconciling its ideas against the kit's rules instead of adopting it
  verbatim:
  - **New organism `UiAdaptiveNavShell`** + value type `UiNavDestination`
    (`lib/src/organisms/ui_adaptive_nav_shell.dart`): a top-level nav scaffold that switches pattern by
    *available pane width* via `UiBreakpoints.classify` inside a `LayoutBuilder` — M3 `NavigationBar`
    for `compact`, compact `NavigationRail` (`extended:false`, `labelType:selected`) for `medium`,
    extended rail for `expanded`/`large`. Controlled selection (`selectedIndex`/`onDestinationSelected`);
    all-optional/additive API. Token-only, zero added deps. Handles the Flutter assertion that an
    extended rail requires `labelType == null` (labels render inline).
  - **Test** `test/ui_adaptive_nav_shell_test.dart` — 8 tests across the four width tiers (driven via
    `setSurfaceSize`, since a plain `SizedBox` clamps to the 800px default surface), rail
    extended/labelType per tier, bottom-bar + rail selection callbacks, body render, and the
    ≥2-destinations assertion.
  - **Skill** `.claude/skills/flutter-adaptive-navigation/SKILL.md` — when to reach for
    `UiAdaptiveNavShell` vs `UiResponsive`/`UiBreakpoints`, the tier→nav mapping, the rejected-patterns
    list, and the why-behind-the-rules (so future changes can override deliberately).
  - **Docs:** `docs/flutter-adaptive-ui-design-specification.md` v3.0→3.1 (new §3.4 shell recipe, §3.5
    reconciliation table vs. the external guide, §9 Future Extensibility); `design-system-contract.md`
    v1.4→1.5 (organism inventory + revision row); `CHANGELOG.md` 0.5.0; `pubspec.yaml` 0.4.0→0.5.0;
    barrel export + doc-comment.
- **Why:** User asked to integrate the external guide. It can't be adopted as-is — it adds four
  runtime packages (`window_manager`/`flutter_adaptive_kit`/`universal_breakpoints`/`fluent_ui`, vs.
  the zero-dependency rule), uses 600/900/1200 (kit uses centralized 600/840/1200), M2
  `BottomNavigationBar` (kit is M3-only), and `Platform.isWindows`-driven layout (kit decides by width,
  not OS). Its four tiers map cleanly onto `UiDeviceClass`, so the kit provides the *shell*; window
  management, keyboard shortcuts, and `go_router` state stay in the consuming app (separation of
  concerns). Additive-only → MINOR bump; consumers pinned to v0.4.0 unaffected until they bump.
- **Refs:** plan `please-review-the-guide-serialized-coral.md` (user-reviewed across two rounds — a
  future-proofness review confirmed the kit approach is already superior/2025-26-aligned and rejected
  adding speculative `desktopNavType`/`breakpoints` params now, keeping the API additive for later).
  Design rationale persisted to memory `adaptive-nav-shell-design`. Verified: `flutter analyze` clean,
  `flutter test` all 102 green.

### [decision] Surfaced (not fixed) stale folder-path guidance for a next plan
- **What:** While checking skill/doc alignment with the current implementation, found three live
  guidance files still describe the pre-v0.4.0 layout (`lib/src/components/`, `lib/src/composite/`)
  instead of `atoms/`/`molecules/`/`organisms/`: `.claude/skills/flutter-ui-kit-component/SKILL.md`
  (heavily — layer tables, §7, references), `.claude/skills/dart-solid-principles/SKILL.md`,
  `.claude/skills/adaptive-workflow/SKILL.md` (one line), and `.ai/best_practices.md`. (Historical
  records — `CHANGELOG.md`, `history/`, `plans/archive/` — correctly reference old paths and were left
  untouched.)
- **Why:** Per user direction, kept this task scoped to the nav-shell work and captured the alignment
  fixes as a separate next plan rather than scope-creeping the commit.
- **Refs:** plan stub `plans/2026-07-15_skill-doc-folder-alignment.md`; ROADMAP Epic 2.

### [doc] docs/flutter-adaptive-ui-design-specification.md v3.1→3.2 · CLAUDE.md — external-guide alignment audit + canonical-guide governance
- **What:** Ran a detailed audit of `UiAdaptiveNavShell` vs. the external "Flutter Complete Adaptive
  Layout Guide" and recorded the outcome (docs-only, no `lib/` change):
  - Spec **§3.5**: appended the remaining *intentional* divergences beyond the existing table
    (hardcoded rail `grey.shade50`, forced `Padding(24)` on content, forced `leading/trailing`
    shrink, demo AppBar tier/platform/width badges) — all dropped per the token-only rule + caller
    flexibility.
  - Spec **§9**: added the two genuine *functional* gaps vs. the guide as designed-for extension
    points (deferred under the promotion rule): an **adaptive FAB** (compact→extended labelled FAB by
    tier, via a future `UiNavFab{icon,label}` descriptor — the reason `floatingActionButton` stays
    additive) and **rail destination tooltips**. Bumped frontmatter + header to **v3.2**, added a
    `### Summary of v3.2 Adjustments` section.
  - **CLAUDE.md**: added a **"Canonical-guide rule"** subsection under Conventions — one authority per
    design area; reconcile any alternative against it and adopt only if *clearly better*, moving the
    source of truth in the same change so two authorities never disagree. Names the current canonical
    authorities (contract + three skills + adaptive spec) and cites the guide-harvest as the worked
    precedent.
- **Why:** User asked to "check in detail what is not aligned with the guide," then to codify a
  golden-rule so future divergent designs are filtered through one must-follow guide/skill. The audit
  found nothing wrong in the kit — divergences are intentional (buckets A/B), deferred gaps (C), or
  places the guide is actually buggy and the kit is correct (D: guide's `labelType:all + extended:true`
  would assert; its 800dp→extended testing row is self-contradictory). Docs-only → no semver bump, no
  CHANGELOG entry.
- **Refs:** plan `please-review-the-guide-serialized-coral.md` (repurposed as the audit; user-reviewed
  twice — chose document-only for the C-gaps, added the canonical-guide governance). Full A–D bucket
  breakdown archived with the plan. Verified: `flutter analyze` + `flutter test` still green (no code
  touched).

### [doc] docs/golden-rule/ · .claude/skills/golden-rule/ · docs/flutter-layout-and-component-design/ — golden-rule folder + safeguard skill + fold the over-budget layout guide
- **What:** Physically established a **golden-rule** home for the canonical rules, added an advisory
  **safeguard skill**, and folded the one doc that exceeds the token budget — all docs/skills/hook-path,
  no `lib/` change:
  - **`docs/golden-rule/`** — `git mv`'d the two canonical docs in (`design-system-contract.md`,
    `flutter-adaptive-ui-design-specification.md`) + new `index.md` that restates the Canonical-guide
    precedence and links every authority (2 docs + the design skills + the new golden-rule skill +
    CLAUDE.md Constitution). Reference/how-to docs (Atomic article, layout guide, ONBOARDING) stay in
    `docs/` (not golden). Updated **every live inbound reference** (from a pre-flight `git grep` audit):
    4 skills, CLAUDE.md, README, `.ai/best_practices.md`, the `supplement.py` `LIVING_DOCS` string
    literal, 2 templates (incl. a GitHub blob URL), and the intra-`docs/` relative links whose depth
    changed (ONBOARDING, the Atomic article's link to the contract, and the contract's `../` link back
    to the Atomic article). Historical records (`history/`, `plans/archive/`, `CHANGELOG.md`) left as-is.
  - **`.claude/skills/golden-rule/SKILL.md`** — advisory safeguard operationalizing the Canonical-guide
    rule: locate the one owning authority → align (fold in) / contradict (may only *replace* if clearly
    better **and** `flutter analyze`+`test` green **and** the authority is updated in the same change) /
    else reject-and-record. States its limits honestly (advisory, not a gate; `flutter test` is the
    breakage gate; it can't mechanically verify semantic consistency). CLAUDE.md's Canonical-guide rule
    now points at `docs/golden-rule/index.md` and this skill.
  - **`docs/flutter-layout-and-component-design/`** — folded the ~9k-token single file (over the
    Progressive Disclosure Semantic ≤5,000 budget) into `index.md` + 6 topic parts (each with a
    `parent:` back-link) + a sibling `CHANGELOG.md` (`exclude_from_ai: true`), per GUIDE §6.4. Content
    preserved verbatim, split by topic. Updated `README.md`, ticked **ROADMAP Epic 2** fold item.
- **Why:** User wanted one physical place for the golden rules + a safeguard so new rules are reconciled
  before adoption (prevent drift), and the big docs chunked for readability. Validated first: a skill is
  *advisory only* (real enforcement needs a hook; even a hook can't judge semantic alignment), the
  governance largely *formalizes the existing Canonical-guide rule* rather than creating a competing
  authority, only genuinely-canonical docs are "golden" (the adaptive spec's golden status is
  *provisional* pending its `official: unknown` provenance — ROADMAP Epic 2), and the physical move was
  done against a full reference audit. Docs-only → no semver bump, no CHANGELOG entry.
- **Refs:** plan `please-review-the-guide-serialized-coral.md` (user-reviewed across four rounds — forks
  decided: physical move, advisory skill, fold only the over-budget guide; pre-flight `git grep`
  confirmed the audit exhaustive incl. `lib/` = zero doc refs). Verified: no stale live references
  remain, `supplement.py` `LIVING_DOCS` path resolves, `flutter analyze` clean, `flutter test` 102 green.

## 2026-07-16
### [doc] .claude/skills/{flutter-ui-kit-component,dart-solid-principles,adaptive-workflow} · .ai/best_practices.md · templates/app-ui-component.SKILL.md.template — align stale folder-path guidance with v0.4.0 Atomic tiering
- **What:** Executed the deferred `2026-07-15_skill-doc-folder-alignment` plan — five live-guidance
  files still described the pre-v0.4.0 layout (`lib/src/components/` + `lib/src/composite/`); mapped
  every present-tense reference to the current Atomic tiers (`components/` → `atoms/`; `composite/` →
  `molecules/` (stateless) or `organisms/` (local UI state)):
  - **`flutter-ui-kit-component/SKILL.md`** (most stale): description frontmatter, §1 layer table,
    §1a atomic table, the molecule-vs-organism framing (now *separate folders* = the state boundary;
    reviewer still gates the judgment for a new widget), §7 headers (merged the two composite headers
    into `molecules/` + `organisms/`), self-check bullet, References link.
  - **`dart-solid-principles/SKILL.md`** — SRP layer list + `ui_button.dart` reference link.
  - **`adaptive-workflow/SKILL.md`** — promotion-target line.
  - **`.ai/best_practices.md`** — layer-rules bullet; version 1.1→1.2 (+ revision row); also synced the
    drifted frontmatter (`version: 1.0`/`last_validated: 2026-07-11` → `1.2`/`2026-07-16`).
  - **`templates/app-ui-component.SKILL.md.template`** — found by the pre-flight full-repo grep, missed
    by the original plan; user chose the full fix. Updated the kit-repo rows **and** the app-side rows
    so consuming apps mirror the kit's Atomic tiering (per CLAUDE.md "the kit **is** the canonical
    Atomic Design system every consuming app mirrors"); also aligned stray "composite"→"composition"
    terminology for coherence. `{{APP_NAME}}`/`{{CONSUMING_PACKAGE}}` placeholders preserved.
- **Why:** v0.4.0 (2026-07-12) reorganized `lib/src/` into strict Atomic tiers, but these guidance files
  still pointed at folders that no longer exist — misdirecting anyone (human or agent) following them.
  Stale-guidance fix, not a code bug: no Dart, no token/API, no semver bump, no CHANGELOG entry.
  Historical records (`CHANGELOG.md`, `history/`, `plans/archive/`) correctly reference old paths and
  were left untouched.
- **Refs:** plan `plans/2026-07-15_skill-doc-folder-alignment.md` (user-reviewed twice; template folded
  into scope after pre-flight grep). Verified: post-edit `git grep "lib/src/components\|lib/src/composite"`
  returns only ROADMAP L17 (the task description) — zero hits in the five edited files.

### [test] test/ui_responsive_test.dart · test/ui_under_maintenance_test.dart · test/ui_tuning_panel_test.dart — close the molecule/organism test-coverage gap (Epic 1)
- **What:** Added the three missing mirrored tests for the barrel-exported widgets that shipped without
  one (contract requires a mirrored test per widget). No `lib/` change.
  - **`ui_responsive_test.dart`** — the four `UiDeviceClass` tiers at representative widths, exact
    boundary flips (599/600, 839/840, 1199/1200) driven via `setSurfaceSize`, and the unbounded-width
    `MediaQuery.sizeOf` fallback branch (child in a horizontal `SingleChildScrollView`, driven by an
    explicit `MediaQuery` so no `MaterialApp` rebuilds it).
  - **`ui_under_maintenance_test.dart`** — icon/title render, `subtitle` default (`'Under maintenance'`)
    vs. override, and token styling (`UiSizing.iconLg` + `theme.disabledColor` read back via
    `tester.widget<Icon>`/`<Text>`).
  - **`ui_tuning_panel_test.dart`** — slider→`UiTuning` setter writeback, external-change rebuild of the
    slider label, `Reset`, and the `Copy as code` path (fresh `SystemChannels.platform` mock via the
    file-scope binding — no prior clipboard-mock precedent in the suite — asserting the clipboard string
    + the confirmation `SnackBar`). Uses the `UiTuning.instance` reset `setUp/tearDown` pattern and a tall
    surface so the lazily-built `ListView` buttons render.
- **Why:** Top open Epic 1 item — all three widgets were consumer-facing but untested. Tests-only:
  no token/API/contract change, so no semver bump and no `CHANGELOG.md` entry.
- **Refs:** plan `provide-the-task-details-steady-scroll.md` (user-reviewed across four rounds; chose to
  leave the one-off clipboard-mock idiom in the test file rather than codify it — promotion rule). ROADMAP
  Epic 1 item ticked. Verified: `flutter analyze` clean, `flutter test` all 115 green (was 102).

### [code] lib/src/theme/ui_theme_controller.dart · lib/src/organisms/ui_theme_picker.dart · example/ · test/ · docs/ — user-selectable theme colors (v0.6.0)
- **What:** Added a runtime, user-facing theme-color selection mechanism so consuming apps can let
  their end users recolor the UI and pick light/dark/system. Built on the existing
  `buildUiTheme(seed:)` lever (no signature change):
  - **New `UiThemeController`** (theme/, `lib/src/theme/ui_theme_controller.dart`) — a release-facing
    `ChangeNotifier` singleton (private ctor + `instance` + `reset()`, mirroring `UiTuning`) holding
    the chosen color `seed` (default `#1565C0`, = `buildUiTheme`'s default) and `themeMode` (default
    `system`). Setters no-op when unchanged. `selectPreset()` convenience. Persistence is deliberately
    the app's job (zero-dep) — `seed`/`themeMode` public for restore + save.
  - **`UiThemePreset` + `kUiThemePresets`** (same file) — curated 2026 palette: Ocean Blue, Emerald,
    Teal, Violet, Cyan (first entry == default seed).
  - **New organism `UiThemePicker`** (`lib/src/organisms/ui_theme_picker.dart`) — `ListenableBuilder`
    on the controller; a `Wrap` of tappable swatch circles (color-as-data fill, selection ring/check
    from `ColorScheme`) + a `SegmentedButton<ThemeMode>` (Light/Dark/System, icon+label).
  - **Example rewired** (`example/lib/main.dart` + `gallery_screen.dart`) as the reference wiring:
    `ViewerApp` now `ListenableBuilder`s on the controller and passes `theme`/`darkTheme`/`themeMode`;
    the gallery app bar's brightness toggle is replaced by a palette button opening `UiThemePicker` in
    a modal bottom sheet (brightness now handled inside the picker).
  - **Tests:** `ui_theme_controller_test.dart` (defaults, notify/no-notify, `selectPreset`, `reset`,
    default==first-preset), `ui_theme_picker_test.dart` (swatch tap → seed, Dark segment → themeMode,
    single check indicator), and extended `ui_theme_test.dart` (custom seed changes `colorScheme.primary`;
    semantic `UiColors` stays seed-independent). Both singleton tests use the reset `setUp/tearDown`.
  - **Docs/versioning:** design-system-contract v1.5→1.6 (theme+organism inventory, organism-reads-
    singleton note, the **color-as-data** carve-out to the token-only rule, `UiThemeController` vs
    `UiTuning` framing, revision row); `CHANGELOG.md` 0.6.0; `pubspec.yaml` 0.5.0→0.6.0; barrel exports
    + doc-comment.
- **Why:** User wanted end-users to switch UI theme colors from a set of modern (2026-researched)
  palettes. Seed-based M3 presets chosen over hand-authored per-palette `ColorScheme`s (minimal,
  zero-maintenance, always consistent); semantic accents kept fixed (brand-independent). Ran the
  golden-rule reconcile first: the pattern *aligns* with the contract (zero-dep, M3, layer/token rules,
  reuses the `UiTuning` singleton shape) — adopted by folding into the contract in the same change; the
  only new nuance (color-as-data) was added there, so no competing authority. Additive → MINOR bump;
  consumers pinned to v0.5.0 unaffected until they bump.
- **Refs:** plan `i-want-implement-those-melodic-island.md` (user-reviewed across 3 rounds — chose
  seed-based presets, controller+presets+picker deliverable, all 5 presets, `themeMode` light/dark/
  system, app-bar bottom-sheet UX, no standalone toggle atom (YAGNI/promotion rule), icon+label
  segments). Verified: `flutter analyze` clean, `flutter test` all 127 green (was 115).

### [code] example/ (windows/, gallery_shell.dart, tier_registry.dart, gallery_widgets.dart, component_demos.dart, main.dart) · .ai/best_practices.md · example/README.md — tablet-ratio Windows gallery with toggleable Atomic-tier nav
- **What:** Built a fast **visual-review template** in the `example/` viewer app — a Windows desktop
  build sized to a 10″ tablet, with a manually-toggleable side nav whose tabs are the five Atomic tiers.
  **Kit untouched** (`lib/` = no change, no semver bump, no CHANGELOG); all work is `example/` + native
  runner + docs.
  - **Windows platform** — `flutter create --platforms=windows .` generated `example/windows/`; edited
    `windows/runner/main.cpp` initial `Win32Window::Size` `1280×720`→**`1280×800`** (10″ landscape,
    16:10). Window stays resizable. Zero native deps (no `window_manager`).
  - **New `GalleryShell`** (`example/lib/gallery_shell.dart`) — the app's new `home`. A plain
    `NavigationRail` (deliberately **not** `UiAdaptiveNavShell`, which is width-driven with no manual
    toggle): a hamburger `IconButton` flips `_navExpanded` → icon-only (`labelType:none`) vs icon+label
    (`extended:true`) regardless of width. Five destinations = the tiers; selected component is sticky
    per tier. Keeps the app-bar `UiThemePicker` + `UiTuningOverlay` actions.
  - **New `tier_registry.dart`** — local (kit-untouched) tier→component mapping: **Atoms** reuse the
    shared `uiComponentCatalog`; **Molecules** = `UiResponsive`; **Organisms** = `UiThemePicker` /
    `UiUnderMaintenance` / `UiAdaptiveNavShell`; **Templates**/**Pages** = a `UiBanner.info` explainer
    (kit has none by design — they live in consuming apps).
  - **New `gallery_widgets.dart`** — extracted `CatalogList` + `PreviewPanel` from the old
    `gallery_screen.dart` (now takes an explicit item list so any tier renders); `git rm`'d
    `gallery_screen.dart` and repointed `main.dart`'s `home`.
  - **`component_demos.dart`** — added interactive `_ResponsiveDemo` (stacked↔row reflow) and
    `_AdaptiveNavShellDemo` (bounded, selectable) keyed `ui_responsive` / `ui_adaptive_nav_shell`.
  - **Docs:** `.ai/best_practices.md` v1.2→1.3 (Windows desktop review target as the primary preview
    path, given the broken `-d chrome` under puro); `example/README.md` v1.0→1.1 (Windows run path +
    tier nav + local molecule/organism mapping).
- **Why:** User wanted to review UI/UX fast without the Android build-install-APK loop, using a Windows
  window at 10″-tablet size so what they see maps to the tablet. `flutter run -d windows` + hot reload is
  the answer (and sidesteps the puro `-d chrome` breakage). Kept strictly in `example/` because
  templates/pages/screens belong to consuming apps, not the kit — so no kit API/token/contract change.
- **Refs:** plan `i-want-you-to-starry-thunder.md` (user-reviewed across 4 rounds — resizable@1280×800,
  explainer placeholders for Templates/Pages, tier mapping kept local, `git rm` old screen + prune
  imports). Verified: `flutter analyze` clean (kit + example), `flutter test` 127 green (kit untouched),
  `flutter build windows` succeeds, built `.exe` launches (Dart VM service up, no crash).

### [workflow][doc] CLAUDE.md · .claude/skills/adaptive-workflow/SKILL.md · .ai/best_practices.md · docs/golden-rule/index.md — branch + PR merge-gate discipline + checklist-driven execution model
- **What:** Overrode the workflow's default "commit & push to `main`" close with a **branch → PR →
  verified-merge** discipline, and reframed the phases as a **master checklist** rather than a rigid
  pipeline. Bindings-only — the pinned `.claude/workflow-core/` submodule is untouched; all rules live
  in repo-local authorities (CLAUDE.md wins on disagreement):
  - **`CLAUDE.md`** — new "Branch + PR merge-gate" subsection under *Git workflow* (pragmatic branch
    scope rule: branch on a substantive change to `lib/`/tokens/version/canonical guidance —
    `docs/golden-rule/`, `CLAUDE.md`, `.claude/skills/`; trivial edits go straight to `main`; branch
    `<type>/<slug>`; PR as the verification gate; `gh pr merge --squash --delete-branch` only on
    user-confirmed verification; conflicts stop-and-ask; `gh` fallback = push + manual PR). Bindings
    section gained the **checklist-driven model** (mandatory gates **G1** git sync · **G2** branch/PR
    gate · **G3** ledger · **G4** no dangling `UNFINISHED.md`, + selectable rows), **F6** Phase-0
    PR/merge gate, and the **Phase-3 branch close** override.
  - **`adaptive-workflow` skill** — mirrors the same (new "Checklist-driven execution + branch/PR
    merge-gate" section) so skill and Constitution stay in sync.
  - **`.ai/best_practices.md`** v1.3→1.4 — distilled git-workflow entry + revision row.
  - **`docs/golden-rule/index.md`** v1.0→1.1 — recorded git-workflow authority = CLAUDE.md + the
    golden-rule **reconciliation record** (incumbent "commit & push to `main`" → adopted "branch → PR
    → verified merge", authority updated same change).
- **Why:** User asked that each plan evaluate whether to branch, close by committing/pushing on that
  branch, open a PR, and — before starting new work — check for an open PR and confirm it's verified
  before merging to `main`; and asked for a *more flexible* checklist (pick what applies) rather than
  always running every step. Consumer apps tag off `main`, so a PR verification gate prevents an
  unverified change from silently breaking a consumer. Ran the golden-rule reconcile: adopted as a
  repo-local override (substantially better here), authority moved in the same change; submodule left
  pinned/untouched. Docs/workflow only — no `lib/`, no semver bump, no CHANGELOG entry.
- **Refs:** plan `glowing-cuddling-flame.md` (user-reviewed across 4 rounds — confirmed pragmatic
  branch scope + assistant-runs-`gh`-merge, added `.claude/skills/` to branch-worthy scope, folded a
  Risks & mitigations table with `gh auth status` verification + stop-and-ask on conflicts). First
  change to exercise the new flow end-to-end (this very branch + PR). Verified: `flutter analyze`
  clean, `flutter test` all 127 green (no code touched).

### [doc] README.md · .ai/best_practices.md · docs/flutter-layout-and-component-design/{part-6-naming-conventions,index,CHANGELOG}.md — reconcile stale folder paths after the v0.4.0 Atomic restructure (ROADMAP Epic 2)
- **What:** Closed the remaining live stale-path references from the v0.4.0 restructure
  (`components/`+`composite/` → `atoms/`/`molecules/`/`organisms/`; `UnderMaintenancePage` →
  `UiUnderMaintenance`). An exploration pass confirmed the `docs/` set was already reconciled (all
  cross-links resolve; contract/adaptive-spec/Atomic-doc use the new tiering) — the genuine breakage
  lived outside `docs/`:
  - **`README.md`** — the "Project structure" tree was fully pre-v0.4.0. Full refresh to mirror
    `ls lib/src/*/` today: `components/`→`atoms/` (all **15** atoms, was 6), `composite/` split into
    `molecules/` (`ui_responsive`) + `organisms/` (nav shell, theme picker, tuning panel/overlay,
    `ui_under_maintenance.dart`), added the `catalog/` layer, and added the two missing `theme/`
    tokens (`ui_tone.dart`, `ui_theme_controller.dart`). Fixed `under_maintenance_page.dart` →
    `ui_under_maintenance.dart`.
  - **`.ai/best_practices.md:60`** — dangling `composite/` promotion-rule prose → "the right kit tier
    (`molecules/` or `organisms/`)"; version 1.4→1.5 (+ revision row).
  - **`docs/.../part-6-naming-conventions.md:34`** — renamed the *consumer-app* example folder
    `composite/` → `screens/` (valid example, but the name now collides with the retired kit layer);
    folder version 1.1→1.2 (index revision row + CHANGELOG 1.2 entry).
  - Left intentional references as-is: `design-system-contract.md:77` (consumer "composite/-equivalent"
    concept) and `:117` (the `under_maintenance_page` anti-pattern name in the no-`_page` rule).
- **Why:** ROADMAP Epic 2 "reconcile the docs set after the restructure so paths resolve." Docs-only —
  no `lib/`, no token/API, no semver bump, no `CHANGELOG.md` entry. Historical records (`CHANGELOG.md`,
  `history/`, `plans/archive/`, `ROADMAP.md` completed rows) correctly preserve old paths, left untouched.
- **Refs:** plan `proceed-eager-gizmo.md` (user-reviewed twice — chose full README refresh + the part-6
  rename). Verified: post-edit stale-path grep over README/.ai/docs returns only intentional hits;
  `flutter analyze` clean, `flutter test` all 127 green (no code touched).

## 2026-07-17
### [doc] docs/golden-rule/{flutter-adaptive-ui-design-specification,index}.md · docs/flutter-layout-and-component-design/{index,CHANGELOG}.md · ROADMAP.md — confirm provenance of the two design docs + de-provisionalize the adaptive spec (ROADMAP Epic 2)
- **What:** Resolved the `official: unknown` / `source: origin unknown` provenance on the two design
  docs the user confirmed are **not** unknown-origin. Per the user: both were **AI-distilled from online
  best-practice resources, then regenerated and reorganized in-repo** into chunked docs → `official: false`,
  `source: agent-generated`. Docs/metadata only, no `lib/` change:
  - **Adaptive spec** (`docs/golden-rule/flutter-adaptive-ui-design-specification.md`) — frontmatter
    `official/source` fixed, `version 3.2→3.3`, H1 `(v3.2)→(v3.3)`, provenance note rewritten to
    "confirmed," + a `Summary of v3.3 Adjustments` (metadata-only) line. Remains a **fully golden**
    authority.
  - **Layout guide** (`docs/flutter-layout-and-component-design/`) — `index.md` frontmatter fixed,
    `version 1.2→1.3` (+ revision row), provenance note rewritten; `CHANGELOG.md` 1.3 entry. Stays a
    **canonical reference** (teaching material), *not* promoted to a rule authority — the contract
    remains the single rule authority (user's explicit choice).
  - **`docs/golden-rule/index.md`** (`v1.1→1.2`) — dropped the adaptive spec's "provisional golden /
    official unknown (ROADMAP Epic 2)" caveat → "fully golden, provenance confirmed"; updated the
    layout-guide "not golden" line to note confirmed provenance; revision row.
  - **`ROADMAP.md`** — ticked the two-docs provenance item (with the **correct** paths: the adaptive
    spec inside `golden-rule/`, the layout guide as the folded folder — the old bare filenames were the
    "noticed the reference but the path is wrong" defect); split the still-pending `Atomic Design in
    Flutter.md` third-party URL into its own open bullet (genuinely external, unlike these two).
  - **Memory:** wrote `canonical-design-docs` (+ MEMORY.md pointer) recording both docs' canonical role,
    correct paths, and confirmed provenance so a future doc reorganization won't lose/mis-path them.
- **Why:** ROADMAP Epic 2 provenance item; the user pointed out these were mis-flagged as unknown and
  had been mis-pathed/left provisional during the earlier golden-rule move + big-doc fold. Light
  content-consistency skim vs. the contract found **no conflict** — the spec's Android `sw` device table
  (600/720, `shortestSide`) is a different axis from `UiBreakpoints`' pane-width classes (600/840/1200),
  a distinction §3.5 already documents. Docs/metadata only → no semver bump, no `CHANGELOG.md` (release)
  entry. `Atomic Design in Flutter.md` left pending (needs its external source URL).
- **Refs:** plan `plans/archive/2026-07-17_confirm-doc-provenance/`. Verified: post-edit
  `grep "official: unknown\|origin unknown"` over the two docs returns only revision-history descriptions
  of the change; `flutter analyze` clean, `flutter test` all 127 green (no code touched).

### [chore] .claude/settings.json — allow scoped gh commands without prompting
- Added `Bash(gh pr:*)`, `Bash(gh run:*)`, `Bash(gh auth status)` to `permissions.allow` so the branch/PR merge-gate workflow (`gh pr list/checks/create/merge`, `gh run` status) stops prompting each call. Scoped deliberately (not blanket `gh:*`) since the file is checked-in/shared — destructive `gh` verbs still prompt. Config only; no `lib/`/token/contract change, no semver bump. Note: the separate auto-mode classifier can still gate a chained/destructive merge regardless.

### [chore] .github/workflows/ci.yml — recursive submodule checkout in CI (ROADMAP Epic 3)
- **What:** Added a `with: submodules: recursive` block to the `actions/checkout@v4` step so CI
  checks out the `.claude/workflow-core/` submodule instead of leaving it empty on a fresh clone.
  One-line-shape config change; `flutter pub get`/`analyze`/`test` steps untouched.
- **Why:** ROADMAP Epic 3 first item — the default checkout skips submodules, making CI's tree an
  incomplete mirror of the working tree (would break any future CI step referencing workflow-core
  hooks/skills/config). Pre-flight verified: submodule remote is **public** (anonymous
  `git ls-remote` succeeds → no `token:` needed); Flutter fully ignores the submodule (no `.dart`
  under `lib/`/`test/`/`example/` references it, no `build_runner`/codegen in `pubspec.yaml`). CI
  config only — no `lib/`, no token/API/contract change, no semver bump, no `CHANGELOG.md` entry.
  Dev-only; consumers via `flutter pub get` unaffected.
- **Refs:** plan `proceed-the-plan-breezy-biscuit.md` (branch `chore/ci-submodules-recursive` + PR).
  Verified locally: `flutter analyze` clean, `flutter test` all 127 green (unaffected). The real
  gate is the green PR CI run showing the checkout step now populates `.claude/workflow-core/`.

### [doc] docs/Atomic Design in Flutter.md (+ golden-rule/index.md, ROADMAP.md)
- **What:** Resolved the long-pending provenance of the `Atomic Design in Flutter.md` reference.
  Original external source URL was deemed **unrecoverable**, so instead of leaving it open the
  content was independently verified for technical accuracy against the repo's Flutter 3.44 / M3
  (online-confirmed: `WidgetState`/`WidgetStateProperty` after 3.19, `withValues(alpha:)` in 3.27+;
  plus five-tier Atomic Design, `ThemeExtension` lerp/copyWith, `textScaler` goldens, 48×48 targets,
  4.5:1 contrast, adaptive `NavigationBar→Rail→Drawer` — all accurate, no conflict with the
  contract) and **adopted as an `agent-verified` repo-owned reference**. Consolidated the sidecar
  `docs/Atomic Design in Flutter.md.prov.md` **into the doc's own in-body frontmatter + a
  `## Revision History` table** (matching every other markdown doc in the repo) and **deleted** the
  sidecar. Refreshed the stale `docs/golden-rule/index.md` "verbatim / provenance pending" note and
  ticked `ROADMAP.md` Epic 2.
- **Why:** The URL was never going to surface; a permanently-open provenance item is worse than an
  honest `agent-verified` adoption. `source: agent-verified` is a deliberate, honest distinction
  from the `agent-generated` docs written here from scratch — externally-originated, independently
  reviewed. Docs/metadata only → no semver bump, no `CHANGELOG.md` (release) entry.
- **Refs:** plan `plans/archive/2026-07-17_atomic-design-provenance/`. Verified: frontmatter parses
  as YAML; sidecar gone; `flutter analyze` clean (no code touched).
