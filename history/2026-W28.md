# 2026-W28  (2026-07-06 – 2026-07-12)

## 2026-07-10
### [workflow] .claude/hooks/ · .claude/skills/adaptive-workflow/ · .claude/settings.json · .ai/ · ROADMAP.md · history/ · plans/ · docs/adaptive-workflow/
- **What:** Self-hosted the **Adaptive Self-Correcting Workflow (v3.3)** in flutter-ui-kit,
  ported and adapted from `scale_tech_insight`'s as-built implementation:
  - Unified Python dispatcher hook (`.claude/hooks/adaptive_workflow_hook.py`) wired to
    SessionStart / PostToolUse / Stop, with kit-specific constants (`LIVING_DOCS` →
    `docs/design-system-contract.md`; `SOURCE_PREFIXES` = `lib/`; `TRACEABLE_MARKERS`
    incl. `test/`, `templates/`).
  - `.claude/settings.json` — hook wiring + a Dart-toolchain permission allowlist
    (`Bash(flutter:*)`, `Bash(dart:*)`, `Bash(git:*)`); no `Bash(python:*)` needed since
    hooks aren't permission-gated.
  - `adaptive-workflow` skill (SKILL.md + `references/{workflow-spec,hook-integration,roadmap-and-archive}.md` + `evals/evals.json`), cross-referencing the existing
    `dart-solid-principles` and `flutter-ui-kit-component` skills.
  - `.ai/` living memory: `env_check.ps1` (Flutter/Dart/Git with a puro-path fallback),
    lean `best_practices.md` and `naming_conventions.md` pointing at the contract.
  - `ROADMAP.md` seeded from verified repo state (v0.1.0); `history/` (this ledger +
    FORMAT.md); `plans/archive/` scaffold; canonical spec copied to `docs/adaptive-workflow/`.
  - Merged the workflow Constitution sections into the existing `CLAUDE.md` (preserving
    all repo-specific content) and updated `.gitignore`.
- **Why:** Multi-session work on the kit needs the same handoff/state/memory discipline the
  workflow provides — surface unfinished plans, keep docs/roadmap current, trace changes.
  Python hooks are dev-only tooling and don't violate the kit's zero-package-dependency rule.
- **Refs:** plan `try-check-on-the-smooth-treasure.md` · canonical spec `docs/adaptive-workflow/`

### [chore] git — committed & pushed the workflow-scaffolding bundle to `origin/main`.
